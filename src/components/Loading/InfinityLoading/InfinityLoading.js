import React from 'react';
import { InfinityLoadingIcon } from '~/assets/svgs';

function InfinityLoading({ wrapperStyle = {} }) {
    return <InfinityLoadingIcon />;
}

export default InfinityLoading;
